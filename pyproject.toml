[project]
name = "template"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pre-commit>=4.3.0",
    "ruff>=0.12.12",
]

[tool.ruff]
target-version = "py312"

[tool.ruff.format]
quote-style = "double"
